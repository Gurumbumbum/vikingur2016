- name: Show folder (debug)
  run: ls -la

- name: Ensure clean install
  run: |
    npm init -y
    npm install playwright google-spreadsheet

- name: Verify install worked
  run: ls node_modules | grep google

- name: Install Playwright browsers
  run: npx playwright install

- name: Run scraper
  run: node scraper.js
